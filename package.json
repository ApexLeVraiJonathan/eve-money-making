{
  "name": "eve-money-making",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "devDependencies": {
    "cross-env": "^7.0.3"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter api dev",
    "format": "pnpm -r run format",
    "lint": "echo \"(add eslint later)\"",
    "db:up": "docker compose -f docker-compose.dev.yml up -d",
    "db:down": "docker compose -f docker-compose.dev.yml down",
    "db:reset": "docker compose -f docker-compose.dev.yml down -v; docker compose -f docker-compose.dev.yml up -d",
    "db:logs": "docker compose -f docker-compose.dev.yml logs -f postgres",
    "db:migrate:dev": "cross-env DATABASE_URL=postgresql://postgres:postgres@localhost:5433/eve_money_dev?schema=public pnpm -F api prisma:migrate:deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.15.0"
}
